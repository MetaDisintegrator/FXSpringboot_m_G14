eureka:
  client:
    serviceUrl:
      defaultZone: http://m-dscv-svc:8761/eureka/

spring:
  cloud:
    config:
      discovery:
        service-id: m-config-svc
        enabled: true
  application:
    name: train-service