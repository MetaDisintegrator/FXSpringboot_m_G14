apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: train-service-hpa
  namespace: fxtravel
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: train-service
  minReplicas: 1
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 60
